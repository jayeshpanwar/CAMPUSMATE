// Centralized import for Chatscope UI Kit styles and shared chat setup
// Import this file in any chat-related component to ensure styles are loaded
import React from "react";
import "@chatscope/chat-ui-kit-styles/dist/default/styles.min.css";

// Centralized place to import Chatscope styles so components
// can simply import this file and theme via CSS variables.
export default function ChatUISetup() {
	return null;
}
// You can add global chat-related JS setup here in future (e.g., theme helpers)

// You can add global chat-related JS setup here in future (e.g., theme helpers)
export default null;
